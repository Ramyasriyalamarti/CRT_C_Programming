#include <stdio.h>
#include <math.h>
int main() {
    int l,b;
    scanf("%d %d",&l,&b);
    int c=0;
    while(l<=b)
    {
        l=l*3;
        b=b*2;
        c++;
    }
    printf("%d",c);
    return 0;
}
